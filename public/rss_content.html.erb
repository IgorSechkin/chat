<% if @feed != nil %>
  <% @feed.items.each do |item| %>
      <div>
        <h1><%= item.title %></h1>
        <%= item.description.strip %>
        <hr>
      </div>
  <% end %>
<% else %>
  <%= @page %>
<% end %>